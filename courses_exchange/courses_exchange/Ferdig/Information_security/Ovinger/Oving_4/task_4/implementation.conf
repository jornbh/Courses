# Simple config-file ( html is most likely defined on a higher level, so we don't need to specify it)



        ssl_certificate         /root/certs/signed_by_dedis_example.com.crt;
        ssl_certificate_key     /root/certs/example.com.key;

        ssl_ciphers         EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH;
        ssl_protocols           TLSv1.1 TLSv1.2;



server{

        listen 443 ssl default_server;
        listen [::]:443 ssl default_server;
        location / {
                root /www/;
        }


}

